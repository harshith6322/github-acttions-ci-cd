name: Triggers Events

on:
  push:
    branches:
      - "main"
      - "feature/**"
      - "master/**"
    
    # branches-ignore:
    #   - "master/**"
    #   - "doc/**"

  pull_request:
    branches: 
     - "main"
     - "feature/**"
     - "master/**"
    types:
      - opened
      - synchronize #updated
      - closed
      - reopened
      - labeled
      - ready_for_review
    paths: 
      - "**"
      - "!*.md"
      - "!doc/**"

  schedule:
    - cron: "45 9 * * *"

  workflow_dispatch:
  
    
    
  
 

jobs: 
  STAGE-1:
    runs-on: ubuntu-latest
    steps:
      - name: step-1"
        run: |
          echo "stage-1"
          date

